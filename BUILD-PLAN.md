# Build Plan

Derived from `PRD.md`. Each phase lists the owning agent, the files it may touch, and objective acceptance criteria. Numbers in brackets like [DoD 4] refer to PRD section 14 Definition of Done items.

**Status legend:** `[ ]` not started, `[~]` in progress, `[x]` done and verified.

---

## Dependency graph

```
Phase 0 Foundation  (done)
      |
      +-----------------------------+
      |                             |
Phase 2 Shell + DOM contract   Phase 1 Data integrity
      |                             |
      +-------+-------+             | (independent, run in parallel)
      |               |             |
Phase 3 Curator  Phase 4 Client     |
      |               |             |
      +-------+-------+-------------+
              |
        Phase 5 Responsive + a11y
              |
        Phase 6 Docs + deploy
```

Phase 1 touches only `data/tools.json`. Phases 2 to 5 touch only code. They share no files, so they run concurrently. Phases 3 and 4 both depend on Phase 2 having frozen the DOM contract and CSS token block, after which they own separate JS files and separate CSS sections and can also run concurrently.

---

## Phase 0: Foundation `[x]`

Owner: main thread. Complete.

- [x] `git init`, remote wired to `hum-ae-n/kai-freeSaaS`, based on `origin/main`
- [x] Directory layout per PRD section 3, `tools.json` moved to `data/`
- [x] `netlify.toml` per PRD section 9, with a note against `force = true` shadowing the data file
- [x] `.gitignore` covering OneDrive and editor artifacts
- [x] `scripts/validate-data.mjs`, zero dependency schema gate
- [x] `CLAUDE.md`, `BUILD-PLAN.md`, `BUILD-LOOP.md`, `.claude/agents/`

---

## Phase 1: Data integrity `[x]`

Owner: **data-steward**. Files: `data/tools.json` only.

Baseline at Phase 0 close: **290 errors across 82 of 85 tools**. This is the single largest risk in the project. Built strictly to spec, the current data renders 165 dead `<a href="">` links in client mode and fails [DoD 8].

| Rule | Count | Meaning |
|------|-------|---------|
| `training-dead-url` | 90 | training entry with no URL |
| `alternatives-min` | 77 | fewer than 2 alternatives that actually link anywhere |
| `alternatives-dead-url` | 75 | alternatives entry with no URL |
| `training-min` | 48 | fewer than 2 training resources that actually link anywhere |

Note that `alternatives-min` (77) is far worse than a raw array length count suggests (23). Many tools carry two entries where only one is a real link.

Tasks:

- [x] 1.1 Amend PRD section 4 to add the optional `notes` field: array of strings, for non linkable caveats. This is a spec change, so make it in `PRD.md` and note it in the changelog at the bottom of this file.
- [x] 1.2 Sweep every `alternatives` and `training` entry with an empty `url`. For each, either supply the real URL or move the text into `notes`. Entries like `"Paid: Adobe Photoshop, Affinity Photo"` are notes. Entries like `"OpenAI Academy"` have a real URL that was simply omitted.
- [x] 1.3 Split the 17 comma joined entries where one `name` lists several products but carries one `url` (for example `{"name": "Coolors.co, Adobe Color", "url": "https://color.adobe.com"}`). Each product becomes its own entry with its own URL, or moves to `notes`.
- [x] 1.4 Top up every tool to at least 2 genuinely linkable alternatives and 2 linkable training resources. Prefer open source and self hosted options, per PRD section 10.
- [ ] 1.5 Spot check `value` figures against PRD section 10 honesty rule. The validator cannot judge this. Flag anything indefensible rather than silently changing it.

Acceptance: `node scripts/validate-data.mjs` exits 0. Tool count stays 85 and the type split stays 15 core, 50 noncore, 7 m365, 13 sector unless a change is deliberate and recorded below.

---

## Phase 2: Shell and DOM contract `[x]`

Owner: **builder**. Files: `index.html`, `css/styles.css`, `js/data-loader.js`, `assets/logo.svg`.

This phase exists to make phases 3 and 4 safely parallel. It ends by freezing a contract that neither may unilaterally change.

- [x] 2.1 `index.html`: document head, Source Sans 3 font link, two empty mount points `<main id="curator-root" hidden>` and `<main id="client-root" hidden>`, module script tag.
- [x] 2.2 `css/styles.css`: the 18 colour tokens from PRD section 11 as `:root` custom properties, base typography, reset. Then two clearly delimited sections, `/* === CURATOR === */` and `/* === CLIENT === */`, which phases 3 and 4 own respectively.
- [x] 2.3 `js/data-loader.js`: fetch `data/tools.json`, parse URL params, route to mode. Exports the loaded tool array and the parsed selection.
- [x] 2.4 URL param parsing, per PRD section 5. Presence of `t` triggers client mode. Handle the cases the PRD leaves open: unknown ids are skipped silently, malformed values are skipped, and `?t=` with zero valid ids renders an explicit empty state rather than a blank page. **Do not use `.filter(Boolean)` on parsed ids**, or tool 0 is dropped.
- [x] 2.5 Fetch failure renders a visible error message, not a blank page.
- [x] 2.6 `assets/logo.svg`: Kaipability logo. If no source asset is available, build a clean wordmark in brand red `#c0392b` and flag it for replacement.
- [x] 2.7 Freeze the DOM contract into a comment block at the top of `js/data-loader.js`: the ids and class names phases 3 and 4 may rely on.

Acceptance: page loads over HTTP in both modes with a visible placeholder, no console errors. Tool 0 survives a round trip through link generation and client mode parsing.

---

## Phase 3: Curator mode `[x]`

Owner: **builder**. Files: `js/curator.js`, the `CURATOR` block of `css/styles.css`.

Per PRD section 6, top to bottom:

- [x] 3.1 Header: logo, title, subtitle, live tool count
- [x] 3.2 Link generator: client name input, "Generate link", copy button, "Preview client view" in a new tab
- [x] 3.3 Filters: type dropdown (All / Core / Non-core / M365 / Sector / Checked only), category dropdown built from the data, search across name, category, description, alternatives and training
- [x] 3.4 Stats bar: selected count, total value equivalent, showing count
- [x] 3.5 Legend for the four type colours
- [x] 3.6 Tools table, nine columns per PRD section 6, row background matching type, hidden rows via `display: none`
- [x] 3.7 Type default states: `core` pre-checked, all others unchecked, per PRD section 4
- [x] 3.8 Action buttons: copy as tab separated, select all CORE, select all visible, deselect all
- [x] 3.9 Copy output in the exact three column format of PRD section 6, with a TOTAL row. Use the async Clipboard API with a visible toast on success and on failure.
- [x] 3.10 Favicons on tool name URLs only. Not on alternatives or training, per the table in PRD section 8.

Acceptance: [DoD 1], [DoD 2], [DoD 3], [DoD 7]. Filters compose correctly, meaning type and category and search apply together rather than overriding each other.

---

## Phase 4: Client mode `[x]`

Owner: **builder**. Files: `js/client.js`, the `CLIENT` block of `css/styles.css`.

Per PRD section 7:

- [x] 4.1 Header: logo, "Your Free Software Stack", "Prepared for [client]", auto generated date, context line. **Insert the client name with `textContent`.** See the security section of `CLAUDE.md`.
- [x] 4.2 Summary bar: tool count and total annual value, value in success green and prominent
- [x] 4.3 Cards grouped under category section headers, ordered as in PRD section 4
- [x] 4.4 Card layout per the PRD section 7 diagram: favicon, name, value, domain list, description, ALTERNATIVES block, GET STARTED block
- [x] 4.5 Render the `notes` array from Phase 1 as plain text, visually distinct from the links
- [x] 4.6 Every link is an `<a>` with `target="_blank" rel="noopener noreferrer"`
- [x] 4.7 Favicons on tool URLs, alternatives and training, with DuckDuckGo primary, Google fallback, `onerror` hide, `loading="lazy"`, `alt=""`
- [x] 4.8 Footer: "Curated by Kaipability Ltd", logo, link
- [x] 4.9 `@media print`: URLs shown after link text, cards do not break across pages, summary bar simplified
- [x] 4.10 `<article>` elements for cards, per PRD section 12

Acceptance: [DoD 4], [DoD 5], [DoD 6], [DoD 8]. Verified at 375px. `?client=<img src=x onerror=alert(1)>` renders as literal text.

---

## Phase 5: Responsive and accessibility `[~]`

Owner: **builder**, gated by **verifier**. Files: `css/styles.css`, plus minimal markup fixes.

- [x] 5.1 Breakpoints per PRD section 11: above 1200px full table and 2 column cards, 768 to 1200px horizontal scroll table and 1 column cards, below 768px compact stacked controls and full width cards
- [x] 5.2 Keyboard: every checkbox, button and link reachable and operable in tab order
- [x] 5.3 Visible focus indicators on all interactive elements
- [x] 5.4 Contrast at 4.5:1 minimum. The token pairs in PRD section 11 need checking, particularly `--noncore-fg #856404` and `--text-3 #888888` on `--card #ffffff`.
- [x] 5.5 Badges carry text labels, not colour alone
- [ ] 5.6 Favicon load volume: 85 tools times several links each can exceed 300 third party requests. Confirm `loading="lazy"` is doing its job, and consider inlining favicons for the 15 core tools.

Acceptance: [DoD 5], [DoD 6]. Verifier signs off against PRD section 12.

---

## Phase 6: Documentation and deploy `[~]`

Owner: **content-editor**, deploy by main thread.

- [x] 6.1 `README.md`: how to add a tool, how to edit one, the URL schema, how to run locally including the `file://` trap, how to deploy, how to run the validator
- [x] 6.2 Connect the GitHub repo to Netlify, confirm auto deploy on push to `main` (site `kai-freestack`, live 23 Jul, status badge in README)
- [ ] 6.3 Custom domain: `tools.airl.io`, deferred to the end by decision 23 Jul. When it lands, update the `og:url`, `og:image` and canonical URLs in `index.html`.
- [~] 6.4 Full Definition of Done pass, all ten items, on the deployed URL rather than localhost. 23 Jul: live files byte-verified against `origin/main`, headers and favicon proxy confirmed; human checks outstanding (favicons on a real phone, copy-paste into Word).

Acceptance: [DoD 9], [DoD 10].

---

## Phase 7: Hardening, social layer and features `[~]`

Post-launch expansion, 23 Jul. Research-driven (two web research passes on 2024-2026 best practice: social unfurling, print, share, motion, WCAG 2.2, directory trust patterns, preset UX, icon licensing). Verifier-gated per batch. Runs on branch `claude/read-todo-bk47qb` until merged.

- [x] 7.1 CI: GitHub Actions runs validator + smoke test on every push to `main` and every PR
- [x] 7.2 Schema: ID permanence rule, optional `archived` flag, optional `last_verified` date (PRD section 4 amended); all 85 tools stamped with the Phase 1 link-sweep date
- [x] 7.3 Social: static OG/Twitter meta before the stylesheet, canonical link, branded 1200x630 `assets/og-image.png` (regenerable from `scripts/og-card.html`), JS-injected `noindex` in client mode only
- [x] 7.4 Hardening: `sanitizeParam` caps `?client=` at 80 chars, whitespace collapses to absent
- [x] 7.5 Print and share: A4 print stylesheet with visible "Print or save as PDF" button, Web Share API with clipboard fallback in both modes
- [x] 7.6 Motion and layout: Carbon-spec card entrance and value count-up behind `prefers-reduced-motion`, single-card categories span full row, sticky stats bar, 44px client buttons, `ul/li` card semantics, trust and provenance lines, freshness badge, archived-tool retirement card
- [x] 7.7 Smoke suite extended 15 to 28 checks covering the 7.2-7.11 surface
- [x] 7.8 Presets: five persona starter packs in `data/presets.json`, one-click chips above the curator filters, editable after apply
- [x] 7.9 Adoption checklist: per-card "Mark as set up" toggle, localStorage per selection hash, progress line, device-only honesty note
- [x] 7.10 `?note=` personal message param (280 chars, sanitized) with curator input
- [x] 7.11 Lucide category icons inline on client section headers (ISC licence)
- [x] 7.12 Content pass: copy polish (header redundancy, summary caption), README feature docs
- [x] 7.13 Final verifier pass over the whole phase plus fresh screenshots (23 Jul: PASS, every commit in the range builds, docs verified truthful against behaviour, contrast pairs all 4.5:1+, ready for merge review)

Deliberately NOT in this phase, decisions reserved for Rocky: public curator mode / `when` column exposure, value figure honesty review (1.5), favicon self-hosting (5.6), analytics provider (Plausible recommended by research), "How we choose" page content.

---

## Phase 8: Dataset truth, cost model, mobile, dark mode, exports `[~]`

Rocky's 23 Jul direction: remove non-SaaS, cover the developer/web gap, model real costs and scaling, fix mobile, smarter entry point, dark mode, branded exports. Verifier-gated per batch on branch `claude/read-todo-bk47qb`; merge only on Rocky's approval.

- [x] 8.1 Schema: pricing trio `free_limit` / `paid_from` / `scales_with` (PRD section 4), validator enforcement, smoke counts derived from data
- [x] 8.2 Data: five non-SaaS entries archived (72, 73, 76, 77, 78), 13 tools added including the new Developer & Web category (98 entries, 93 active). Borderline non-SaaS flagged for Rocky, not archived: 61 HMRC, 65 NCSC, 66 ICO, 74 training academies, 83 sector training
- [x] 8.3 Data: pricing trio researched and populated for all 80 active pre-phase tools, `last_verified` bumped. Sketchup free tier found to be non-commercial only; three `value` figures flagged to the 1.5 review (66 ICO high, 29 Hotjar low, 82 Sketchup licensing)
- [x] 8.4 Mobile curator: under 768px the table renders as type-tinted stacked cards with 44px targets and a details "More" section; desktop unchanged. Verifier PASS
- [x] 8.5 "Start here" need chips filtering by intent, ghost-styled, one active at a time, manual edits clear. Verifier PASS
- [x] 8.6 Cost model: per-card free-tier line and pricing pill, "How costs could grow" hand-built SVG chart (four team sizes, tooltip on hover and focus, table fallback, print-safe). Verifier PASS, model arithmetic independently recomputed
- [x] 8.7 Dark mode: top-right toggle, warm dark token palette, stored choice, system default, print always light
- [x] 8.8 Branded exports from the curator selection: CSV, standalone HTML snapshot (DOM-built, self-contained), Save as PDF via print param, Email this stack mailto draft
- [x] 8.9 Content pass: README/TODO refresh, PRD section 15 already updated, changelog rows
- [x] 8.10 Final verifier sweep: PASS, merge-ready. Smoke suite extended to 32 checks (dark mode, exports), six pre-existing comment em dashes cleaned, every commit in the range builds.

Decisions this phase surfaced for Rocky, additive to the list above: archive or keep the borderline non-SaaS five; the three challenged value figures; whether Vercel stays listed given its Hobby tier bans commercial use.

---

## Phase 9: Alternatives audit and build-your-own `[x]`

Rocky's 23 Jul direction after the Phase 8 look: HubSpot needed Zoho and more; suggest building your own where realistic; full review of alternatives coverage. Verifier PASS 23 Jul (relaunched once after a worker restart killed the first run mid-flight).

- [x] 9.1 Schema: optional `byo` string (PRD section 4), validator enforcement, house-style sweep extended to `byo` and `free_limit`
- [x] 9.2 Render: "Or build your own" sage-ruled block on client cards between alternatives and get started, in the standalone HTML export, and as a CSV export column; XSS-verified in page and snapshot
- [x] 9.3 Data: full alternatives audit, roughly 35 tools gained verified entries (Zoho CRM, Odoo, EspoCRM on HubSpot per Rocky; Ollama, LinkStack, Documenso, Akaunting, Coolify, Colima, Whisper among the open-source additions). Affinity relaunched free October 2025: stale paid notes on ids 2-5 converted to live links
- [x] 9.4 Data: nine `byo` entries (27, 42, 44, 45, 48, 49, 50, 52, 97), each stating what is realistically buildable and where the real tool earns its keep; none on security, e-signing, accounting, deliverability or commerce. Copy-voice variety pass after verifier feedback
- [x] 9.5 Smoke suite: byo presence/absence checks added (34 checks total)
- [x] 9.6 Verifier findings closed: CSV byo column, README field list, this section

Flagged for the Phase 1.5 value review: 27 Looker Studio possibly undervalued, 4 GIMP's comparator reasoning shifted now Affinity is free.

---

## Phase 10: Programme, engine, plain English `[~]`

Rocky's 24 Jul direction: all eight level-up ideas approved, plus Plain English mode ("easier for Ralph Wiggum") and surfacing pricing/BYO in the curator, which are client-mode-only today. Wave structure below respects file ownership; verifier per wave; PR preview then Rocky merges.

Wave 1:
- [~] 10.1 Schema: optional `plain` field, a one-line plainest-words description per tool (PRD section 4, validator, house-style sweep)
- [~] 10.2 Data: steward writes `plain` for all active tools, same honesty bar, 12 words or fewer where possible
- [~] 10.3 Curator surfacing: pricing pill and a BYO marker visible in the table (value cell subline, mobile More details gain Free tier and Build your own)
- [~] 10.4 Edit an existing stack: "Open in curator" from client mode via ?edit= param, preselects and prefills name and note
- [~] 10.5 Single-tool permalink: ?tool=ID renders one card with minimal chrome

Wave 2:
- [x] 10.6 Plain English mode: client toggle plus ?plain=1, swaps descriptions to `plain`, relabels sections in plain words, cost section reframed "How free can turn into paying" with a one-sentence takeaway
- [x] 10.7 Progress share-back: "Share progress with Kaipability" mailto carrying the set-up ticks
- [x] 10.8 QR bridge: self-generated QR (no third party) on the printed page linking to the live stack

Wave 3:
- [x] 10.9 Link-rot Routine: weekly sweep of all URLs and the 15 stalest pricing claims, proposes fixes on a maint/ branch, never touches main. NOTE: this lives OUTSIDE the repo, as a Claude Code Routine (trigger trig_01WxNYBCqWQGJx6Wq6Dsv5Ub, Mondays 03:00 UTC, push notification to Rocky on completion); the repo intentionally carries no workflow file for it. Verify via the Routines list, not the codebase.
- [x] 10.10 Recently-updated strip from tools.json git history (removals shown, per the trust research)
- [x] 10.11 Embed mode via a dedicated /embed path with a scoped frame-ancestors exception for kaipability.com
- [x] 10.12 Public/staff split, Rocky's 24 Jul decision resolving the public-curator question: root becomes a public read-only directory (cards, trust strapline, How we choose, one CTA); curator moves to the hidden path /x, noindexed, absent from any sitemap, and deliberately absent from robots.txt (a disallow line would advertise the path). Existing ?t= client links unchanged. The client-page "Open in curator" button renders only on staff devices (a local flag set by visiting /x), so the path never leaks to clients. Curator-generated links always point at the root path.
- [x] 10.13 Content pass done, verifier sweep PASS, PR open for Rocky

---

## Phase 11: My Stack account register workspace `[x]`

Specified by `PRD-REGISTER.md` (authoritative). Owner: **workspace-builder** (new agent) for js/my/* and the WORKSPACE css block; content-editor for prose; verifier gates every wave. Informed by three research passes: storage-durability landmines (the file-is-primary doctrine), frontend/product inspiration (Watchtower tiles, consequence sentences, recessive sidebar, ledger costs), backend and security posture (store-adapter seam, PBKDF2 600k, strict CSP, privacy copy). Branch until Rocky's PR review, as ever.

- [x] 11.0 Spec: PRD-REGISTER.md authored; CLAUDE.md gains the fourth surface and register laws; workspace-builder agent created
- [x] 11.1 Wave A (verifier PASS 25 Jul): app shell and routing (/my, noindex), store adapter (single choke-point, versioned whole-document blob, IndexedDB + localStorage mirror), Web Locks + revision refusal + BroadcastChannel, crypto core with CI test vectors (scripts/register-vectors.mjs wired into ci.yml), first-run gates (webview refusal, private-mode sentinel), example register, setup flow ending in a verified export, lock/unlock
- [x] 11.2 Wave B (verifier PASS 25 Jul, 89 independent checks): register table (five columns, drawer, completeness meters, risk chips, bulk owner edit), stack import via ?from=, sovereign templates, Overview tiles with click-through filters and the no-owner bucket
- [x] 11.3 Wave C (verifier FAIL then fix round, focused re-verify PASS 25 Jul): Leavers checklist generator (five-phase preservation-to-destruction order, printable, persistent ticks), Costs ledger (renewals lists, monthly/annual toggle, existing chart), My tools screen, backup-age indicator and nags, iOS share-file export path. Fix round closed a leaver-name stale closure, made encrypted export verification a true decrypt round-trip, and enforced 44px targets on the import and reassign controls
- [x] 11.4 Wave D (verifier PASS 25 Jul, two carry-forwards into 11.5): awareness page ("Why we built this", evidence-led, one commercial sentence maximum), privacy notice verbatim via shared js/my/copy.js, strict CSP with hashed boot scripts, Referrer-Policy no-referrer on /my, security.txt, client-page and public-directory entry points. Carry-forwards: (1) Netlify header-merging for the layered Referrer-Policy and dual CSP on /embed.html is simulated locally only, must be confirmed with curl against the PR Deploy Preview before merge; (2) smoke/CI coverage of the CSP hashes, security.txt and why-register.html lands in 11.5
- [x] 11.5 Close-out (final sweep verdict READY FOR PR, 25 Jul): content pass (README/HOW-TO/TODO; workspace copy needed no changes), smoke suite extended 54 to 74 checks (CSP hash drift gate, security.txt, why-register, /my DoD mechanics: first-run, CRUD persistence, two-tab conflict refusal, export/wipe/re-import, backup-age thresholds), example-register tool links corrected (Canva 2, Slack 96, Meta 38), ?from= accepts the documented t: prefix, final verifier sweep against PRD-REGISTER section 15 all items PASS, PR opened for Rocky. Deploy Preview header checks (Referrer-Policy layering on /my, dual CSP on /embed.html, security.txt content type) are the merge gate

---


## Phase 12: Homepage, Discover deck, batch add, sign-up lists, reading-copy exports `[ ]`

Rocky's 26 Jul direction: public homepage redesigned for new visitors with tasteful animation (mobile leads with a Discover deck, desktop leads with browse plus grid quick-judge); swipe left "got it", right "add to my list", judgements persisting locally; My Stack batch account add; a sign-up to-do generator with opt-in planned-row pre-seed; CSV/TXT/PDF reading copies of the register. Specified by PRD sections 16-17 (PRD v1.4) and PRD-REGISTER sections 16-21 (v1.1), both authoritative. Informed by the Phase 12 research pass (finite 10-12 card decks, buttons-first per WCAG 2.5.7, pointer-event physics without a rAF loop, `freestack:v1:discover` persistence, editorial hero trust signals, full reduced-motion fallback). Owner split as ever: **builder** for the public surfaces, **workspace-builder** for `/my`, **content-editor** for prose, **verifier** gates every wave. Branch until Rocky's PR review, as ever; Phase 11 and all follow-ups are merged, so Phase 12 branches from main.

Sequencing is deliberate: waves 12.1-12.3 ship the deck usable on its own before any register-side work; 12.4-12.6 are workspace-builder waves and sequential (same owner, same files). CSP note for every wave: **no new inline scripts** in `index.html` or `embed.html`; the deck lands as the external module `js/discover.js`, so the hashed-script set in `netlify.toml` must be byte-identical at close-out. If a wave is ever forced to add an inline script, its hash lands in `netlify.toml` in the same commit with a changelog row, and 12.8 re-verifies both files' CSP against the Deploy Preview.

- [x] 12.0 Spec commit (landed 26 Jul as 5667546; the close-out sweep confirmed versions, numbering, enum and changelog rows, and caught that this box was never ticked). Owner: main thread with content-editor. Append PRD sections 16-17 and bump to v1.4; append PRD-REGISTER sections 16-21, apply the section 4.2 status-enum edit in place, bump to v1.1; add this phase block and the changelog rows below. Verifier gate: numbering continuity, both version headers bumped, no em dashes, section 4.2 enum reads `planned`, `active`, `to-close`, `closed`.

- [x] 12.1 Wave A, homepage redesign (verifier FAIL then fix round, focused re-verify PASS 26 Jul; the fix round cleared a permanent transition-delay leak into the hover lift and made reveals fire once per visit rather than on every redraw, and the re-verify's test-quality finding hardened both regression checks). Owner: **builder**. Files: `index.html`, `js/public.js`, the PUBLIC block of `css/styles.css` (plus an empty, delimited DISCOVER block for 12.2). Hero with runtime tool count, no-affiliates line and curator identity; three entry paths (Discover, persona chips from `data/presets.json`, browse all); viewport ordering (deck entry leads below 768px, list leads above); motion inventory items 1-3 (60-80ms stagger, once-only IntersectionObserver reveals, hover lift) with the full `prefers-reduced-motion` opacity-only fallback. Smoke checks added: hero count equals the active count derived from `data/tools.json`; reduced-motion query yields no transform transitions on reveal elements; homepage carries no robots meta; 375px no horizontal scroll; `netlify.toml` CSP hash set unchanged. Verifier gate against PRD section 16 layout, motion and platform rules.

- [x] 12.2 Wave B, deck engine (verifier FAIL then five-fix round, focused re-verify PASS 26 Jul: atomic per-card input lock against double-judging, pointer capture deferred past slop so the More link clicks again, fling velocity from move samples only, site-wide [hidden] override which also cured a latent curator linkgen-result visibility bug, pointercancel always aborts). Owner: **builder**. Files: `js/discover.js` (new), `js/public.js` (mount and open/close wiring only), the DISCOVER block of `css/styles.css`. Deck seeding (persona pack, category, default mix), 10-12 card deal from unjudged active ids, buttons and keyboard primary (Left/Right/Backspace/Esc), pointer gesture with 10px slop, `touch-action: pan-y`, 100px-or-35%-width-or-0.5px/ms commit thresholds, transform-only physics (about 1 degree per 20px, origin bottom centre, no rAF loop), single-level undo with animate-back, `aria-live` polite announcements, progress counter, completion card, `freestack:v1:discover` persistence (`{v, lastVisit, seenIds, decisions}`), new-to-you counts, hand-off URL `/my?from=&have=` with `have=` always emitted and the 512-char cap. Directory must render even if `js/discover.js` fails to load. Smoke checks added: keyboard-judge tool 0 and find `decisions["0"].d === "have"` after reload; a second deal excludes judged ids; deck length never exceeds 12; completion link contains `have=` (including the empty-value marker case) and never a skipped id; sub-threshold drag records nothing; Esc restores focus to the opener; localStorage-blocked run still deals and completes. Verifier gate against PRD section 17 in full.

- [x] 12.3 Wave C, list parity and quick-judge (verifier FAIL, two fix rounds, closed PASS 27 Jul: chooser gained outside-click and Escape-from-chip dismissal with one-open-at-a-time; the fragile positional card pairing was replaced by a data-id lookup, one authorised line in client.js; the measured corner overlay was rearchitected twice, landing as a reserved normal-flow rail where overlap is impossible by construction, with data-driven longest-name smoke checks at three widths plus a resize case). Owner: **builder**. Files: `js/public.js`, `js/discover.js` (judgement read/write stays in discover.js as the one owner of the key), `css/styles.css`. State chips on judged browse cards with the Got it / Add to my list / Clear chooser on every device; corner quick-judge buttons under `(hover: hover) and (pointer: fine)` only; persona chips seed decks; 44px targets throughout. After this wave the deck is shippable alone: 12.4 onward must never become a merge blocker for 12.1-12.3. Smoke checks added: deck decision renders as a list chip and clearing via the chooser removes it from the stored decisions; quick-judge corners absent under coarse-pointer emulation; chip and corner targets measure 44px or more. Verifier gate against PRD section 16 parity rules plus a regression pass over 12.2.

- [x] 12.4 Wave D, planned status and Discover arrival (verifier FAIL then fix round, focused re-verify PASS 27 Jul: the planned filter moved inside leaverChecklist as the single source of truth, planned-only owners left the leaver dropdown, free-text names gained the honest no-live-accounts line, and Costs gained the same planned exclusion with a changelog row). Owner: **workspace-builder**. Files: `js/my/workspace.js`, the WORKSPACE block of `css/styles.css` (store.js untouched: no new methods, no schema bump). `planned` in the status enum end to end (chip, "To sign up" group, drawer, risk-tile and Leavers exclusion); `?have=` parsing beside `?from=` (shared `parseSelection` after `t:` strip, 512-char raw cap on both); import review groups ("Already using these" active, want group defaulting planned only when `have=` is present in the URL); one `store.save()` per commit; legacy `?from=`-only links byte-for-byte unchanged in behaviour. Smoke checks added: `/my?have=0` commits tool 0 as one active row in one revision; `/my?from=2,5&have=0` defaults 2 and 5 to planned; `/my?from=t:0,2,5` without `have=` matches pre-phase behaviour; a 600-char `from=` value is ignored; grep gate that no `js/my/*` file references `freestack:v1:discover`. Verifier gate against PRD-REGISTER sections 16 and 19.

- [x] 12.5 Wave E, batch add, sign-up generator, reading-copy exports (verifier PASS 27 Jul with one defect fixed same day: the bulk bar's buttons joined the 44px mobile floor with a pixel-measuring smoke check; the two-tab conflict mid-batch, hostile-formula CSV parsing, real lock cycle and pre-seed dedupe all held first time). Product question resolved by Rocky 27 Jul: a duplicate batch pick creating a second row is fine as is, consistent with manual add. Owner: **workspace-builder**. Files: `js/my/workspace.js`, `js/my/templates.js` (template picks in the batch multi-select), WORKSPACE css including print-sheet rules. Batch add per PRD-REGISTER section 17 (multi-select over catalogue, templates and free text; one shared identity/owner/mfa form; one save for the whole batch; drawer overrides after). Sign-up generator per section 18 (four-point checklist with `free_limit` verbatim, in-page print-sheet pattern from `printRecoverySheet`, copy-as-text with toast, opt-in pre-seed as planned rows in one save, `toolId` dedupe with id 0 handled). Reading-copy exports per section 20: CSV and TXT via `Blob` plus the existing `downloadBlob` pattern, OWASP formula-injection escaping (leading apostrophe on `=` `+` `-` `@` tab, full quoting, CRLF), TXT grouped like the register table, PDF strictly as the in-page print sheet labelled "Print or save as PDF" (no library, per the no-dependency law), all three placed on Backup below the register file with the "these cannot be imported, only json can" line, never counting as a verified export (`savesSinceExport` and backup-age untouched), and unavailable while locked. Smoke checks added: batch of 3 increments revision by exactly 1; generator output contains the `free_limit` sentence for a tool that has one; pre-seeding twice yields no duplicate `toolId` rows; CSV cell `=2+5` gains a leading apostrophe and quotes; producing a CSV leaves `savesSinceExport` unchanged; locked register exposes no reading-copy control; grep gate for password-shaped fields extended over the batch form, generator and all three outputs. Verifier gate against PRD-REGISTER sections 17, 18 and 20.

- [x] 12.6 Content pass (27 Jul: new Phase 12 UI copy needed no changes, already in house style; README gained the Discover section, the from/have URL schema rows and the corrected check count; HOW-TO gained the Discover hand-off and sign-up list operator sections; TODO carries the batch-duplicate question for Rocky). Owner: **content-editor**. README (Discover, judgement persistence honesty, batch add, sign-up lists, reading copies), the cannot-be-imported line and "saved on this device" phrasing audited against the section 3 storage doctrine (no "safe" claims anywhere new), Plain English labels for every new control, Cyber Essentials wording check on the generator, house-style sweep (no em dashes, British English) across all new copy and comments.

- [x] 12.7 Close-out (final sweep verdict NOT READY on one deck scroll race, fixed same day with preventScroll and proven against the pre-fix commit, independently re-run by the main session; the sweep hand-drove the full cross-wave journey and every DoD item; smoke suite ended the phase at 173 checks; PR opened for Rocky with the Deploy Preview header regression list). Owner: main thread, gated by **verifier**. Full sweep against PRD sections 16-17 and PRD-REGISTER section 21; smoke suite extended from 74 checks to cover every wave's additions with the new count recorded here; both themes, print, 375px; CSP verification that the `netlify.toml` hashed-script set is unchanged (or the recorded exception re-verified) with curl against the Deploy Preview, same as the Phase 11 merge gate; PR opened for Rocky.

---

## Phase 13: Payments, first rung `[ ]`

Rocky's 29 Jul direction: a first payment feature as a learning exercise, provider-agnostic. Build instructions live in `docs/PAYMENTS.md` (the spec for this phase): hosted checkout links only, no SDK, no CSP change, no backend, provider swappable by editing one constants file. Gated on Rocky completing Stripe setup and supplying the live payment link URLs; the site side is one small builder wave plus the usual verifier gate. The binding trust rule: payments never influence listings, and the no-affiliates line must remain literally true.

- [~] 13.0 Rocky: Stripe account and tip payment link, GoCardless account and audit paylink, per docs/PAYMENTS.md section 3 (decided 29 Jul: both providers, one per product). (Stripe done 2 Aug: Kaipability Ltd account `acct_1U076QCfSXPoTFR8` activated, GB/GBP, charges and payouts enabled, live tip Payment Link supplied. GoCardless still pending business verification, so the audit entry ships empty and renders nothing. Two Dashboard items outstanding for Rocky: the public business name still reads "Kaipability Ltd. sandbox", which appears on hosted checkout pages and receipts, and Bacs Direct Debit is not yet enabled, which matters for advisory invoices rather than the tip.)
- [x] 13.1 Builder wave per docs/PAYMENTS.md section 4 (js/payments.js constants, public footer links, trust sentence, smoke checks), verifier gate, PR. (Built as `d12fa7f`, verifier FAIL on one load-bearing gap, fixed in `c2ac2bc`. The gap was mine: the commit message claimed the shipped-state checks pinned the live URL against a silent edit, and they did not, being self-referential against the imported module. The verifier proved it by mutation, emptying the tip URL and swapping in a different buy.stripe.com link both passed the full suite. A literal EXPECTED_PAYMENT_URLS pin now closes it, re-proven against those exact mutations. Everything else passed on first verify: no SDK, script, form or iframe anywhere, netlify.toml byte-identical with the CSP hash set unchanged, the provider URL present in exactly one file, tools.json untouched so the no-affiliates line stays literally true, payment links proven absent from client deliverable pages, ?tool=, /x, /my, embed.html and all four static pages, and the empty-state rule verified across all four url combinations. The budget reconciliation was judged honest rather than gate inflation, with the verifier independently measuring 2230/3094/862.67 and confirming --s-1 is genuinely the smallest spacing token. Suite 331 to 356.)

---

## Phase 14: Compact landing, felt motion, answer-engine visibility `[ ]`

Rocky's 29 Jul direction: the huge scrolling opening page does not work, the animations are imperceptible, and the directory should pull AI answer engines and people searching for free software. Specified by PRD section 16 as amended and new section 18 (PRD v1.5, authoritative). Informed by the two Phase 14 audits: design (138 mobile screens tall, motion budget spent on unseen entrances) and AEO (AI crawlers execute no JavaScript, so the raw HTML contains no directory content). The two are reconciled: all 89 cards stay in the rendered DOM behind CSS-collapsed shelves, and the static crawler block is orthogonal, serving non-JS crawlers before the app boots. Owner split as ever: **builder** for the public surface, **content-editor** for prose, **verifier** gates every wave. Branch until Rocky's PR review. CSP note for every wave: no new executable inline scripts; JSON-LD data blocks are the one recorded exception to the smoke gate (PRD section 18), and 14.4 re-verifies both CSP values against the Deploy Preview.

[Rocky] dependencies: (1) publishing `docs/how-we-choose.md` as `/how-we-choose.html` is IN 14.3 but gated on his copy sign-off, conditional line below; (2) confirmation of the positioning phrase "free software for UK small businesses", which the title, meta and FAQ copy all repeat.

- [x] 14.0 Spec commit. Owner: main thread with content-editor. Apply the Phase 14 PRD amendment verbatim: version header to v1.5 with the 30 Jul date line, section 16 layout and motion-inventory replacements, new section 18; add this phase block and the changelog rows below. Verifier gate as 12.0: numbering continuity, version header bumped, no em dashes, the seven-item motion inventory present with its exact tokens, the ten FAQ answers byte-identical to the amendment text. (landed as commit `4e95157`; the close-out sweep caught this box left unticked)

- [x] 14.1 Wave A, compact landing. Owner: **builder**. Files: `js/public.js`, the PUBLIC block of `css/styles.css` (`js/client.js` untouched; `index.html` untouched this wave). Per amended PRD section 16: tightened hero; ways-in band with search promoted first-class (count-bearing placeholder), Discover entry and persona chips, Browse-all card retired for an Expand all / Collapse all toggle; discover mount and changelog strip as today; 15 fully collapsed shelves wrapping `buildCardSections()` output, each a real button header (icon, name, count, truncated scent line of tool names, chevron, `aria-expanded`/`aria-controls`, 44px minimum); search and persona filters force-open matching shelves, hide the rest, show "N tools match", and still compose; `#cat-<slug>` deep links; empty FAQ slot section reserved for 14.3; footer unchanged. Shelf state transient per page load, no new storage keys. Smoke checks added: collapsed page height at most 3,200px at 375px and 2,200px at 1280px, with the search input and first shelf rows inside the first mobile viewport; `#public-root` card count equals the active count from `data/tools.json` (89 today) with all shelves collapsed; searching "claude" opens the AI Assistants shelf and clearing restores collapse; tool id 0 found by search and by its shelf; expand all / collapse all round-trips; a `#cat-` link opens and scrolls to its shelf; `?tool=ID` unchanged; shelf headers are buttons with correct `aria-expanded` at 44px or more; judgement parity (chip, chooser, id 0) still passes; no horizontal scroll at 375px; `netlify.toml` CSP hash set byte-identical. Verifier gate against amended section 16 layout, shelf mechanics and height budgets. (verifier FAIL then fix round interrupted by a worker restart and resumed, focused re-verify PASS 31 Jul: fold recovered from 1185px to 863px via the chip scroll row, toolbar fold and a design-system line-height leak, spec clause reconciled to a pinned 880px budget judged honest; the stray card-in replay suppressed with scoped CSS, client mode untouched)

- [x] 14.2 Wave B, motion. Owner: **builder**. Files: `design-system/colors_and_type.css` (the two easing tokens only), `js/public.js`, `js/discover.js` (open-morph only, deck internals untouched), `js/data-loader.js` (theme toggle wrapped in the guarded helper), `css/styles.css`. The seven-item inventory of amended PRD section 16, with its exact durations and easings: recalibrated hero/ways-in stagger, shelf-expansion stagger, guarded `withViewTransition()` helper on filter redraws and expand-all (feature-detected, debounced, skipped under reduced motion), deck-open `view-transition-name: discover-panel` morph with focus after `finished` and fallback exactly today's behaviour, judged-chip pop on fresh judgements only (`.is-new` in the setDecision path), theme-toggle cross-fade, hover lift with oxblood border shift plus site-wide `:focus-visible`. Smoke checks added: reduced-motion emulation samples `getComputedStyle` during shelf expand, deck open and chip pop and finds no translate, scale or rotate anywhere, with theme toggle and filters instant; a stubbed non-VT browser (feature-detect forced false) completes every interaction identically minus the transition; easing tokens present in `colors_and_type.css`; `.is-new` never applied during load-time redecoration; CSP hash set unchanged. Verifier gate against the amended motion inventory, item by item, in both themes. (verifier PASS 31 Jul on all seven items with mid-flight sampling and an exhaustiveness sweep; the four-line token comment in the design-system file was waived as within the two-token constraint's intent and is recorded here; the fast-first-tap harness rework was judged faithful-to-reality)

- [x] 14.3 Wave C, AEO. Owner: **builder** with **content-editor**. Files: `scripts/build-seo.mjs` (new), `data/category-intros.json` (new), `index.html` (static block between `seo-static` markers, title/meta rewrite, JSON-LD), `faq.html` (new), `sitemap.xml` (new), `llms.txt` (new), `robots.txt` (Sitemap line), `js/data-loader.js` (hide `#static-root` on app mount), `scripts/smoke-test.mjs` (JSON-LD exclusion in `extractInlineScripts`, with the CSP-data-block comment), `.github/workflows/ci.yml` (drift step: run the generator, fail on any diff), `netlify.toml` untouched unless faq.html needs a header rule. Per PRD section 18 in full: deterministic, escaped, id-0-safe generator output; the static block; `faq.html` with the ten canonical Q&As verbatim; the JSON-LD set; title and meta with live count, OG tags unchanged. Content-editor owns the category intros and any connective prose, at the section 10 honesty bar. [Rocky, conditional] on his sign-off of `docs/how-we-choose.md`: publish it as `/how-we-choose.html` in the `why-register.html` mould minus the noindex, link it from footer and FAQ, add it to the sitemap; without sign-off the sitemap ships without it and a TODO row records the gap. Smoke checks added: local server raw fetch of `/` (no JS) contains every active tool name and the trust lines; `#static-root` hidden after app mount; `/faq.html` serves the ten Q&As with JSON-LD strings matching visible copy; sitemap lists only the permitted URLs; robots.txt carries the Sitemap line and no disallow; hash gate passes with JSON-LD present and still fails on a planted executable inline script; running the generator twice is byte-identical; noindex boundaries unchanged (`/x`, client links, `/my`, `why-register.html`); no em dashes in any generated file. Verifier gate against PRD section 18 acceptance criteria 1-9. Includes the per-tool questions of PRD section 18 (Rocky's 30 Jul addition): one generated question-and-answer per active tool in the static block and on the ?tool= permalink view, never in FAQPage JSON-LD. (built in two rounds, verifier PASS 31 Jul with two pre-close-out fixes applied same day: tool 75 paid_from corrected so its generated answer stopped contradicting itself, the answer-length rule reconciled to smoke-enforced 30-100 hard bounds, and summary elements joined the focus-visible rule; XSS, drift, determinism, noindex boundaries and the honesty audit all held)

- [x] 14.4 Close-out. Owner: main thread, gated by **verifier**. Full sweep against amended PRD section 16 and section 18; smoke suite count recorded here (from 173); both themes, print, 375px; reduced-motion pass hand-driven, not only sampled; Deploy Preview checks with curl as a non-JS crawler would fetch: raw `/` HTML contains the directory content, `/faq.html` serves the canonical copy, `sitemap.xml` and `llms.txt` resolve with sensible content types, CSP values on `/` and `/embed.html` byte-identical to `netlify.toml`, noindex boundaries confirmed live; PR opened for Rocky with the [Rocky] items (how-we-choose sign-off state, positioning phrase) called out in the description. (Closed 31 Jul: sweep found two blockers, both fixed and re-proven. The first-open coach dismissal dropped keyboard focus to body, killing all four deck keys; dismissCoach() now restores panel focus, guarded by two smoke checks proven to FAIL on the pre-fix tree, one forced onto the reduced-motion path after the verifier showed the View Transition first-interaction refocus masked it. The README/HOW-TO tool-edit workflow gained the missing build-seo.mjs step. Smoke suite recorded at 275, from 173 at Phase 13 close. Deploy Preview on PR 13 verified by curl and by a real browser fed the preview's bytes: raw no-JS HTML carries all 15 category sections and 89 per-tool Q&As, faq/sitemap/llms serve with correct content types, CSP on all four paths byte-matches netlify.toml, robots.txt carries the Sitemap line, noindex boundaries hold (JS-injected on /x, ?t=, ?tool= and /my; absent on /), docs still 404, and the client XSS probe renders as literal text. One preview-only observation: Netlify's Pretty URLs optimisation rewrites the footer /faq.html link to /faq, which resolves correctly to the same page.)

---

## Phase 15: Header nav, good-practice pages, Discover emphasis (Rocky, 31 Jul: "my stack link should be on top menu bar... better footer privacy statement and other good practice pages like contact us page links to www.airl.io and www.kaipability.com... Discover mode button also needs stronger animations")

Spec: PRD section 16 amended 31 Jul (hero utility nav, footer good-practice block, motion inventory item 8). Company facts gleaned from www.airl.io: Kaipability Ltd, registered in England and Wales, Company No. 15772934, contact info@kaipability.com; airl.io is the AIRL (AI Readiness Level) framework site, also Kaipability's.

- [x] 15.0 Spec. Owner: main thread. PRD section 16 layout items 1 and 6, motion inventory item 8 (bounded two-pulse Discover emphasis, sheen on hover, spring press; explicitly not looping or ambient), section 18 sitemap clause gains privacy.html and contact.html. This block and the changelog rows below. (Landed as `c23cd80`.)
- [x] 15.1 Wave A, structure and motion. Owner: **builder**. Files: `js/public.js`, PUBLIC block of `css/styles.css`, `privacy.html` (new), `contact.html` (new), `scripts/build-seo.mjs` (sitemap and llms.txt lists only), `sitemap.xml`/`llms.txt` (regenerated), `js/client.js` and `js/my/workspace.js` only at the existing footer-link integration points, `scripts/smoke-test.mjs`. Hero utility nav (My Stack, FAQ; zero added height at 375px, 44px targets, part of the first-paint stagger); footer legal/practice line (Privacy, Contact, FAQ, Why we built My Stack) and company identity line with kaipability.com and www.airl.io outbound links; Discover button emphasis per inventory item 8; the two static pages in the why-register.html mould with the byte-identical theme-boot script, indexable, truthful per the amended PRD clause. Smoke checks: nav present with 44px targets and first-shelf budget still at most 880px; both pages serve locally with the shared theme-boot hash and no new inline scripts; sitemap lists exactly /, faq.html, privacy.html, contact.html; footer links carry correct rel; emphasis animation absent under reduced motion; pulse count bounded (animation-iteration-count never infinite anywhere on the button). (Landed as `0c6e1d7`. Verifier FAIL 31 Jul on one defect: item 8's "hidden with the rest of the ways-in band while the deck is open" clause was never implemented, the button and its glow sat above the open deck and the pulse could fire mid-deck. Fix round landed as `528d004`: the whole band hides on deck open, deferred past the morph's finished promise, restored through discover.js's onClose seam before focus returns, with a settled class preventing pulse replay; four new smoke checks proven to fail pre-fix. Known trade, per the clause's own wording: search is unavailable while the deck is open; a visitor closes the deck (Escape or the close button) to search. The verifier's minor flags are recorded here too: `js/my/workspace.js` extends that file's pre-existing target=_blank convention to the new internal links, kept for local consistency; the privacy page's storage list was completed in 15.2. Focused re-verify verdict noted at 15.3.)
- [x] 15.2 Content pass. Owner: **content-editor**. Privacy and contact copy at the section 10 honesty bar (device-only storage, no analytics, hosting provider access logs without naming the provider, favicon proxy requests named plainly, no password language anywhere); README repo map rows for the two pages; TODO banner. (Landed as `7146c65`, alongside the main thread's hardening of the 14.2-era shelf-stagger smoke check, which raced the animation clock under load and now records transitionstart evidence installed before the click, and the privacy storage-list completion: every localStorage key the code writes is now named, including the plain English preference and the staff-device flag.)
- [x] 15.3 Close-out. Owner: main thread, gated by **verifier**. Full smoke, budgets, reduced motion hand-driven on the emphasis sequence, both themes, 375px; Deploy Preview checks on the PR (new pages' content types, CSP on the new paths, sitemap drift gate green); Rocky's merge word. (Closed 1 Aug: focused re-verify PASS on the band-hide fix, with its one non-blocking concern, the pulse racing the View Transition's finished promise under load, closed same day by moving suppression to click time, verified with two suites running concurrently. Deploy Preview on PR 14 audited by curl and a real browser fed the preview's bytes: pages, sitemap, CSP, nav geometry, deck-open hide, Escape restore, reduced motion all clean. Merged as `6d84458` via PR 14 on Rocky's word; production verified byte-identical after the deploy. Suite recorded at 297, from 275 at Phase 14 close.)

- [x] 15.4 Phone-test fixes (Rocky, 1 Aug, PRD v1.7). Owner: **builder**, gated by **verifier**. Three findings from Rocky's live-site phone test:
  1. **Static-block flash on load.** The section 18 crawler block painted for the whole tools.json fetch. Fix per the amended section 18 clause: the shared theme-boot head script stamps a class on the document root before first paint; a static CSS rule hides `#static-root` under that class; the boot failure path removes the stamp. The boot script's new hash replaces the old one in `netlify.toml` in the same commit (this changelog records it), across every page sharing the script; the smoke hash gate updates in step.
  2. **Sticky shelf headers** per the amended section 16 shelf mechanics: an open shelf's header sticks to the viewport top at every width so collapse never needs a scroll back; panel background, separation shadow only while stuck, stacking below the deck.
  3. **Discover button rebuilt to the house CTA treatment** per rewritten motion inventory item 8 (the recorded looping exception, airl.io reference): drifting three-stop brand gradient, soft pulse, periodic sheen, hover pause-lift-glow, press scale; loop resumes after deck close (the old settled/no-replay guard retires); everything off under reduced motion.
  Smoke checks: no frame paints `#static-root` on a JS-capable load (screenshot or first-paint sample) while a blocked-tools.json load leaves it readable; the new boot hash appears in netlify.toml and every page's boot script hashes to it; an open shelf scrolled to its last card still shows its header on screen and clicking it collapses; the button carries infinite iteration counts under normal motion and none under reduced motion; deck-open still hides the band and restore resumes the loop. (Built as `f8790ab`, verifier PASS 1 Aug with independent hash recomputation across all five pages and embed, the netlify.toml diff proven to be exactly the two sanctioned substitutions, flash fix driven under a delayed and a blocked fetch, heading semantics proven to survive display:contents via the accessibility tree, gradient-on-hover proven in both themes past the .btn-primary cascade, contrast 6.93:1 at the lightest stop, the looping exception proven scoped by a whole-page iteration-count sweep, and both new checks spot-proven to bite in a worktree. Suite 307, two clean runs. The builder found two real traps en route: the .btn-primary hover cascade flattening the gradient, worst in dark mode, and the heading wrapper's auto-height box giving sticky no room to pin, solved with display:contents. The reopen-stagger check joined the transitionstart-evidence pattern pre-emptively. One low-risk zero-wait sample remains in the entrance-replay check, judged failure-biased rather than masking, left alone.)

- [x] 15.6 Mobile finesse (Rocky, 1 Aug: "just have Title and Number of tools... the first card should shake to show it can move. It also should have a counter"). Owner: **builder**, gated by **verifier**. Per amended PRD section 16 layout item 4 and section 17 deck composition: shelf headers drop the scent line (icon, title, count, chevron only; the Close hint keeps its slot); the deck's top card runs a one-time rotate-and-return shake on each deck open (after coach dismissal or immediately when coachDone, never during a drag, skipped under reduced motion); the progress counter becomes plainly visible beside the card at every viewport, body-size or larger. Files: js/public.js, js/discover.js (shake trigger and counter placement only, deck physics untouched), PUBLIC and DISCOVER blocks of css/styles.css, scripts/smoke-test.mjs. Smoke: no .pub-shelf-scent in the DOM; header still 44px with count present; shake animation runs once on open (animationstart evidence) and not under reduced motion nor during an active drag; counter visible within the viewport at 375x812 with the deck open, font-size >= body; budgets hold; suite count recorded. (Built as `29f28bf`, verifier PASS 1 Aug with zero findings. Deck physics driven for real: swipes, keyboard, undo, a full deck to completion with tool id 0 surviving the hand-off, persistence intact across reload. The builder's exit-transition fix proven load-bearing by revert, 248ms transition path vs 329ms timeout fallback, on the transform-only have/want exits. The pointer-guard and no-scent checks proven to bite by deletion and re-insertion. Budgets unchanged at 862.67/2986/2190, honestly reported: the scent lived inside the 44px row, so removal freed width not height. Suite 331, five clean runs across builder, main thread and verifier.)

---

## Phase 16: Hero, top bar, changelog page `[x]`

Rocky, 2 Aug: "remove the recently updated and move it" to a subpage; "buttons for plain english and light dark mode switch should be on the top bar really"; "the first section should be distinctive and say what Free Stack is, call it what it is... this is the hero so should be animated and impressive with a nice background". Spec: PRD v1.8 (section 16 layout items 1 and 3 rewritten, motion inventory item 9 added as the second and final looping exception, section 18 sitemap clause).

- [x] 16.0 Spec. Owner: main thread. PRD v1.8 as above, this block, the changelog row below.
- [x] 16.1 Wave A, changelog page and top bar. Owner: **builder**. Files: `scripts/build-seo.mjs` (generate `changelog.html` from `data/changelog.json`, add to sitemap), `changelog.html` (generated), `sitemap.xml`/`llms.txt` (regenerated), `js/public.js`, PUBLIC block of `css/styles.css`, `scripts/smoke-test.mjs`. Remove the changelog strip and its loader from the homepage entirely; move the Plain English and theme toggles into the utility bar beside My Stack and FAQ, retiring the ways-in toolbar row; footer legal line gains Changelog. Net fold cost of the bar must be zero or negative, measured and reported. Smoke: no changelog node on `/`, `changelog.html` serves the entries statically with no runtime fetch, generator byte-stable, sitemap lists exactly the five permitted URLs, all four bar items at 44px, plain English still toggles descriptions and persists, theme still persists and cross-fades, budgets hold. (Landed as `0841fee`. Retiring the shelf-band controls row more than paid for the bar: the first shelf header at 375x812 moved from 862.67px to 724.67px, freeing 155px for the hero wave. The builder flagged that ci.yml's drift gate did not list the new generated `changelog.html`, correctly declined to edit a file outside its wave, and the main thread fixed it in the same commit.)
- [x] 16.2 Wave B, hero. Owner: **builder**. Files: `js/public.js`, PUBLIC block of `css/styles.css`, `scripts/smoke-test.mjs`, `scripts/build-seo.mjs` (the static block's hero copy must match). New headline and sub-line per PRD section 16 item 1 with the runtime count; motion inventory item 9's drifting planes, CSS only, 20s or slower, `pointer-events: none`, static composed frame under reduced motion. Smoke: headline and sub present with the runtime count and no hard-coded 89; contrast measured over the animation's extremes in both themes, not just at rest; no layout shift and no horizontal scrollbar from the planes; reduced motion leaves zero animations on the hero; the exception stays scoped (no third element gains an infinite animation); first-shelf budget still at most 880px at 375x812. (Built as `4a08164`, closed out as `e60f394` after three rounds on the background, the instructive one being the main thread's own error. The verifier caught the contrast check covering two of four hero text lines; the main thread then set the wrong repair standard, demanding the composited all-four-planes-overlapping bound pass, which is unreachable given their authored spans and drove opacity 0.07 to 0.02 to 0.013, erasing the effect the wave existed to build. Replaced with ground truth: hide the hero text, screenshot, decode the PNG and read real composited pixels behind each rendered line box at 10 offsets across the slowest plane's 35s travel, both themes, both viewports, worst pixel per line. That immediately found what neither the arithmetic nor the manual estimate had, that 0.07 was never safe either, and that the binding element was the utility nav links rather than the trust line. Root cause was the colour, not the texture: `--ink-3` is the palette's faintest tint at about 5.4:1 on plain paper, so the hero's nav links and trust line moved to `--ink-2`, an accessibility gain on its own terms. Planes ship at 0.11, eight times the rejected value, worst real pixel 8.57:1 light and 8.56:1 dark against a 4.5 floor. A source-level guard forbids `opacity` inside the drift keyframes, since a runtime sample on a 35s loop always lands near its minimum and would miss the breach; proven to bite by mutation.)
- [x] 16.4 Fixed, self-compressing top bar (Rocky, 2 Aug: "this should be as a fixed top menu bar can compress to burger on scroll down"). Owner: **builder**, gated by **verifier**. Per PRD section 16 layout item 1's fixed-bar clause. Files: `js/public.js`, PUBLIC block of `css/styles.css`, `scripts/smoke-test.mjs`. The bar becomes `position: fixed` at every width and compresses to a burger disclosure once the hero is scrolled past. The known collision: open shelf headers stick at `top: 0` today and would hide under the bar, taking the collapse control with them, so both must be driven by one shared custom property for the bar height. Burger is a real button with `aria-expanded`/`aria-controls`, focus into the panel on open, Escape closing and returning focus, outside click closing. Scroll state via sentinel and IntersectionObserver, never a tweened scroll-linked effect. Smoke: bar visible at scroll depth on `/`, all four controls reachable in both states, the burger disclosure contract proven end to end including Escape and outside-click, a stuck shelf header's full box sits below the bar and is clickable at depth, no control is ever covered, budgets hold, reduced motion leaves the compress instant. (Landed as `1658ea3`. `--topbar-h` drives both the bar height and the shelf headers' `top`, verified live at 44px expanded and 36px compressed. Full burger disclosure contract passing: aria-expanded, aria-controls, focus into the panel, Escape closing and returning focus to the burger, outside click, all four controls still 44px inside the panel. Fold landed at 879.5px against the 880 cap, which is honestly too tight to be comfortable and is flagged for Rocky rather than reconciled unilaterally.

  The wave's real cost was elsewhere. Running the suite five times instead of two exposed FOUR checks that were unreliable, none of them caused by this work: the reduced-motion stamp check sampled computed style 30ms after a commit to prove an absence; the shake pointer-guard raced Playwright's own round trip against a 450ms timer while its comment confidently explained why it could not; the Discover scroll check asserted scrollY moved when the claim was that the deck became visible, reproduced under 15 concurrent pages and shown to break the pre-16.4 code identically; and the residual transition-delay check demanded an empty inline string while its own name asked only that no delay survive, failing on a value of zero. All four now assert outcomes or record events, each proven still to catch its regression by mutation. Five consecutive clean runs at 431 checks.

  Process note worth keeping: this work was briefly lost when a builder stashed it while the main thread declined to commit a red tree. Recovered from the stash intact. On a feature branch a committed red tree with a note beats an uncommitted clean one, because uncommitted work is the only kind that can be lost.)
- [x] 16.3 Close-out. Owner: main thread, gated by **verifier**. Full smoke, budgets, both themes, reduced motion hand-driven, Deploy Preview checks (changelog.html content type and crawlability, sitemap drift gate, CSP unchanged), PR and Rocky's merge word. (Verifier PASS on Phase 16 with two rigour gaps that were fixed rather than waived, both recorded at 16.2. Suite 356 to 403 across the phase. Shipped to Rocky as one release on PR 18 alongside Phase 13.1 payments and the build-notes folder, since every push to the branch updates the same PR.)

---

## Phase 17: Savings ticker `[ ]`

Rocky, 3 Aug: "a roller that spins that shows the maximum amount of money you save in pounds and Starbucks coffees... a ticker on the hero section". Spec: PRD section 16 layout item 1's savings-ticker clause.

- [ ] 17.0 Spec. Owner: main thread. The clause above, this block, the changelog row.
- [ ] 17.1 Build. Owner: **builder**, gated by **verifier**. Files: `js/public.js`, PUBLIC block of `css/styles.css`, `scripts/build-seo.mjs` (the static crawler block states the same figures), `scripts/smoke-test.mjs`. A count-up-once roller in the hero showing the ceiling figure, always paired with the realistic core-twelve figure and a coffee equivalent that states its own divisor. Every number derived at runtime from `tools.json`; no hard-coded totals anywhere. Reduced motion renders the final values instantly with no counting. Smoke: figures match a freshly computed sum of the active `value` fields (mutate the data in a worktree and prove they follow); the ceiling never appears without its "up to"/"all 89" framing or without the core figure; the coffee line names its divisor; no element gained an infinite animation, so items 8 and 9 remain the only two; reduced motion leaves zero animations on the ticker; budgets hold at the reconciled figure.

---

## Open spec questions

Raised by the PRD review and not yet resolved. Each needs an answer before the phase that depends on it closes.

1. **Curator mode is public.** Anyone who visits the bare domain gets the full curator interface including the `when` column of internal consulting guidance. PRD section 13 defers auth to post v1. Confirm that the `when` text is acceptable for public view, or hide that column until auth exists. Blocks Phase 6.
2. **Favicon proxy dependency.** Two third party services with no SLA sit in the render path of every link. Fallback is handled, but decide whether to self host icons for core tools. Blocks Phase 5.6.
3. **`X-Frame-Options: DENY`** in `netlify.toml` will block the `?embed=true` iframe mode listed in PRD section 13. Fine for v1, flagged for whoever builds embed mode.
4. **Value total defensibility.** PRD section 10 sets an honesty standard with no verification mechanism, and an AI builder cannot judge what a comparable CRM costs. Treat as a human review step in Phase 1.5.

---

## Changelog

Record deliberate deviations from the PRD here so the spec and the build stay reconciled.

| Date | Change | Reason |
|------|--------|--------|
| 2026-08-03 | Phase 17.1: the three PRD section 16 page-height budgets reconciled from 880px/3,200px/2,300px to 1,050px/3,350px/2,400px (first shelf header at 375x812, total at 375, total at 1280) | The savings ticker is a genuine block of hero content beneath the sub-line, Rocky's own request, so it pushes every shelf below it down the page exactly as the fixed top bar did at Phase 16.4. Measured at 995.5px, 3,263px and 2,328px respectively; each rounded up with headroom rather than pinned to the exact measurement, the same convention the two existing reconciliations in this budget's PRD paragraph already use. Not spent effort shrinking the hero to claw the numbers back: the brief was explicit that Rocky asked for the ticker and the fold breach this once is expected |
| 2026-08-03 | PRD v2.0 (Phase 17): a savings ticker in the hero, counting up once on arrival, showing the ceiling figure paired with the realistic core-twelve figure and a coffee equivalent that states its own divisor | Rocky asked for "a roller that spins that shows the maximum amount of money you save in pounds and Starbucks coffees". Two constraints written in rather than assumed. First, the ceiling may never appear alone: the `value` field sums to about £11,600 across 89 tools nobody adopts, and section 10 calls a figure nobody would pay a bug the validator cannot catch, so it ships labelled as a ceiling and always beside the core twelve at about £1,900. The pairing is what makes the big number honest instead of a promise, and honesty is this site's actual product. Second, it counts up ONCE and rests: a perpetually spinning roller would be a third ambient exception after item 9 was recorded as the final one, whereas an arrival count-up belongs to item 1's first-paint family and needs no exception at all. The coffee line names its divisor rather than asserting what any chain charges, the same standard `value` itself is held to |
| 2026-08-02 | PRD v1.8 (Phase 16): section 16 hero rewritten to lead with what Free Stack is; the changelog strip leaves the homepage for a generated `/changelog.html`; the utility bar takes the Plain English and theme toggles; motion inventory gains item 9, drifting hero planes, the second and final looping exception | Rocky's 2 Aug direction. Two judgement calls recorded rather than left implicit. First, no money figure in the hero: the `value` field sums to about £11,600 across 89 tools, but nobody adopts 89 tools, so leading with it would be exactly the indefensible number section 10 warns the validator cannot catch; the core twelve at about £1,900 is the honest figure if one is ever used. Second, the mobile fold wins over hero scale: the 880px first-shelf budget is measured at 863 with 17px of slack, so the hero's impact is spent on background and type rather than height, and the desktop hero gets the room the phone cannot spare. An animated background is by definition the ambient motion the inventory bans, so it is written in as a scoped exception with a static reduced-motion frame rather than smuggled past the law |
| 2026-08-02 | PRD section 16 page-height budget at 1280px reconciled from 2,200px to 2,300px (smoke constant updated in step) | Phase 13.1's live tip link and trust sentence measured the page at 2,230px. The footer has grown from three lines to seven across Phases 11, 15 and 13.1, each line deliberately requested; the CSS is already at minimum spacing, so absorbing 30px meant deleting asked-for content. The 880px first-shelf budget, measured 863px and untouched, is what actually protects the compact landing this budget exists for: a footer below every shelf cannot make the top of the page feel long. 375px is unchanged at 3,200 and measures 3,094. Headroom left for the pending GoCardless audit line. Flagged for a later copy pass: the standalone "Curated by Kaipability Ltd." line now duplicates the company identity line beneath it and could merge, which would buy back roughly 20px honestly |
| 2026-08-01 | PRD v1.7 (Phase 15.4, Rocky's phone test): section 18 static block hidden before first paint via a stamp from the shared theme-boot head script, whose new hash replaces the old in `netlify.toml` per the inline-script law; section 16 shelf mechanics gain sticky open-shelf headers; motion inventory item 8 rewritten from the bounded two-pulse emphasis to the airl.io-style ambient house CTA treatment, the inventory's single recorded looping exception, primary CTA only | Rocky, 1 Aug, from the live site on his phone: the crawler block flashed for the whole fetch; an eleven-tool shelf needed a scroll back up to collapse; and the bounded pulse read as "really flat and uninspired, look at airl.io" against the reference CTA, a direct instruction that supersedes the v1.6 bounded design |
| 2026-07-31 | PRD v1.6 (Phase 15): section 16 hero gains a utility nav (My Stack, FAQ; zero added height so the 880px budget stands), the footer gains a legal/practice line (privacy.html, contact.html, both new and indexable) and a company identity line (Kaipability Ltd, Company No. 15772934, links to kaipability.com and www.airl.io), motion inventory gains item 8 (bounded two-pulse Discover button emphasis with hover sheen and spring press), section 18 sitemap clause adds the two new pages | Rocky's 31 Jul direction: My Stack on the top menu bar, a proper footer privacy statement and contact page linking the two company sites, stronger Discover button animation. The emphasis is specified as a bounded sequence because the inventory's ban on looping and ambient motion stays in force |
| 2026-07-22 | Added optional `notes` field to the section 4 schema | The `{name, url}` shape had nowhere to put non linkable caveats, so 165 of them were parked in `alternatives` and `training` with empty URLs, which would render as dead links |
| 2026-07-22 | PRD v1.2: added section 7 Security subsection (textContent / escaping rule for URL params and data) | Client name from `?client=` rendered via innerHTML would be an XSS vector on a branded domain |
| 2026-07-22 | PRD v1.2: section 8 favicon fallback made real (delegated error listener, DuckDuckGo then Google then hide) | Spec named Google as fallback but the snippet only hid the image, so the fallback never ran |
| 2026-07-22 | `--text-3` darkened from PRD section 11's `#888888` to `#6b6b6b` | `#888888` on white is 3.5:1, failing the section 12 requirement of 4.5:1 |
| 2026-07-22 | Added `DESIGN-SYSTEM.md` and `scripts/smoke-test.mjs` beyond the PRD section 3 file list | Token/component documentation, and a repeatable headless-Chromium gate covering DoD 1-8 (15 checks: both modes, filter composition, link round trip including tool 0, XSS literal rendering, empty state, 375px overflow). Favicon rendering and real-world contrast remain human checks per BUILD-LOOP |
| 2026-07-22 | PRD v1.3: styling source of truth is now `design-system/` (Kaipability brand system found in-repo). Cream/oxblood/ink palette, Sonny Gothic + Galano Grotesque self-hosted, editorial flat surfaces. Supersedes PRD section 11's provisional Source Sans 3 / `#c0392b` palette, the section 4 pastel badge colours, and the interim `assets/logo.svg` placeholder (deleted; real lockup + mark adopted) | The user's design system landed in `design-system/` mid-build; the PRD palette was always a stand-in |
| 2026-07-22 | PRD v1.2: categories consolidated 23 to 15 (Security merged into Security & Compliance; Design & Creative, Image & Stock, Image Utilities merged into Design & Images; Analytics & SEO plus Local SEO into SEO & Analytics; Social Media, Email Marketing, Publishing, CRM into Marketing & CRM; Business Support plus Grants & Programmes into Grants & Business Support) | 23 categories over 85 tools produced one-tool client-mode sections and near-duplicate headers |
| 2026-07-23 | PRD section 4: ID permanence rule, optional `archived` boolean, optional `last_verified` ISO date | Client links carry only ids, so deleting a tool silently mutates every deliverable ever sent; archived tools now render an explicit retirement card |
| 2026-07-23 | Added `data/presets.json`, `assets/og-image.png`, `scripts/og-card.html`, `.github/workflows/ci.yml` beyond the PRD section 3 file list | Persona starter packs (research: persona-named bundles beat category lists), social unfurl image, and a CI gate so a web-UI edit cannot deploy unvalidated |
| 2026-07-23 | OG meta targets the client-mode reader on the shared URL; `noindex` is JS-injected in client mode only | Crawlers do not run JS, so per-mode static meta on one URL is impossible without SSR; the client link is the one that gets shared, and a static noindex would deindex curator mode too |
| 2026-07-23 | PRD section 4 amended: pricing trio added to the schema, `free_limit` (string), `paid_from` (integer, GBP/month), `scales_with` (enum: users/usage/features/none) | A tool's own `value` figure said nothing about what happens once a business outgrows the free tier; Rocky's Phase 8 direction asked for the cost-growth story to be modelled, which needs machine-readable pricing data per tool, not just prose |
| 2026-07-23 | Added the Developer & Web category, 13 tools including GitHub, Vercel, Netlify, Cloudflare, Hugging Face and Supabase | Rocky's Phase 8 direction identified developer and web infrastructure as a gap in the catalogue |
| 2026-07-23 | Archived the five Grants & Business Support entries (72, 73, 76, 77, 78) via the section 4 `archived` flag; the category now holds only archived entries and no longer appears in either mode | Grant and support-body listings drift out of date faster than software free tiers and were judged out of scope for a software directory; archiving rather than deleting keeps any client link that already included one resolving |
| 2026-07-23 | PRD section 15 rewritten to state the current 98-entry, 93-active dataset and its type/category breakdown, superseding the "85 tools, 15 core/50 noncore/7 m365/13 sector" figures from Phase 1 | The Phase 8 additions and archiving changed every number in the old section 15 summary; left uncorrected it would read as a false description of the shipped data |
| 2026-07-23 | PRD section 4 amended: optional `byo` string added to the schema (Phase 9.1) | Rocky's Phase 9 direction asked for a build-your-own steer where a lightweight replacement is genuinely realistic; the schema had nowhere to hold that advice separately from the `alternatives` list, so it needed its own field with its own house-style and category exclusions (never security, e-signing, accounting or deliverability) |
| 2026-07-24 | PRD section 4 amended: optional `plain` string added to the schema (Phase 10.1) | Client mode's Plain English toggle needs a genuinely plain-words version of each description to swap to, not a shortened version of the normal one; a separate field keeps the honesty bar on `description` intact while giving plain mode its own honest text to show |
| 2026-07-24 | Public/staff URL split (Phase 10.12), superseding PRD section 5: the root path `/` is no longer curator mode, it is a public, indexable directory of every active tool; the staff curator moved to the unlisted path `/x`, noindexed and deliberately absent from `robots.txt`. Existing `?t=` client links are unaffected, since client mode was never path-dependent | Rocky's 24 Jul decision resolving the open "curator mode is public" question (BUILD-PLAN "Open spec questions" item 1): the bare domain showed the internal `when` guidance to anyone who found it; hiding the staff interface behind an unlisted path removes that exposure without adding authentication, which PRD section 13 defers to post v1 |
| 2026-07-24 | Added `embed.html` and `js/public.js` beyond the PRD section 3 file list (plus `js/qr.js`, `scripts/changelog.mjs` and the generated `data/changelog.json`) | `embed.html` is the chrome-free entry point for iframing the directory on kaipability.com (Phase 10.11); `js/public.js` renders the new public directory at `/` (Phase 10.12); `js/qr.js` generates the print-only QR bridge (Phase 10.8) without a third-party service; `scripts/changelog.mjs` derives the "recently updated" strip from git history rather than a hand-maintained file (Phase 10.10) |
| 2026-07-24 | `netlify.toml` header rules restructured: `X-Frame-Options: DENY` scoped explicitly to `/`, `/index.html` and `/x` rather than sitting on `/*`, with a new `Content-Security-Policy: frame-ancestors` rule scoped to `/embed.html` only | Embed mode (Phase 10.11) needs exactly one page on the site to be framable, and only from kaipability.com; `X-Frame-Options` has no per-origin allow-list and a wider Netlify header rule cannot be overridden by a narrower one, so the blanket DENY had to be split apart rather than exempting embed.html from it |
| 2026-07-24 | Smoke suite reworked for the public/staff split and extended to 50 checks; the suite's local test server now mirrors Netlify's SPA fallback (unknown paths, `/x` above all, serve `index.html`) | The old suite assumed the bare URL was always curator mode, which the Phase 10.12 split broke; a plain static file server also can't exercise `/x` without the SPA-fallback behaviour production actually relies on |
| 2026-07-25 | `js/my/store.js` `save()` gained an `opts.forcePlain` flag beyond the PRD-REGISTER section 6 interface | Disabling encryption has to write the register back as plaintext in the same call that clears the session key; without the flag the store would re-encrypt with the key it is in the middle of discarding. Single call site, `commitDisableEncryption` in workspace.js, and the flag is refused when encryption is still configured |
| 2026-07-26 | PRD-REGISTER section 11 privacy notice amended: "Our hosting provider, Netlify, briefly keeps" becomes "Our hosting provider briefly keeps"; `js/my/copy.js` updated in the same commit so the rendered notice and the spec stay byte-identical | Rocky's 26 Jul direction during phone testing: no need to name the hosting provider in client-facing copy. The honesty content of the sentence (access logs, IP addresses, roughly 30 days, no analytics, no tracking cookies) is unchanged |
| 2026-07-25 | `js/my/store.js` gained a `wipe()` method outside the six-method PRD-REGISTER section 6 seam | "Delete everything on this device" (section 9, Backup screen) must clear IndexedDB, the localStorage mirror and in-memory state atomically through the same choke-point as every other write; doing it from workspace.js would mean a second module touching storage keys directly, which section 6 forbids more strongly than it fixes the method count |
| 2026-07-26 | PRD v1.4: sections 16 (Public homepage) and 17 (Discover deck) added; the Phase 10.12 "one CTA" homepage description is superseded by the three entry paths | Rocky's 26 Jul Phase 12 direction: the root page is redesigned for first-time visitors, with the deck as the discovery path and the browse list as the reference path |
| 2026-07-26 | New public-surface localStorage key `freestack:v1:discover`, deliberately outside the `js/my/store.js` choke-point, which governs `/my` persistence only; hand-off to the workspace travels solely in the URL (`?from=` plus new `?have=`) | Deck judgements are public-surface working data, not register data; letting `/my` read the deck's key would create a second storage-touching module, which PRD-REGISTER section 6 forbids |
| 2026-07-26 | PRD-REGISTER v1.1: sections 16-21 added; the section 4.2 `status` enum gains `planned`; schemaVersion stays 1 | The register had no honest status for an account that is intended but not yet opened, which both the sign-up generator and the Discover want-list hand-off need; additive enum value, so existing documents and exports remain valid |
| 2026-07-26 | `?from=` and `?have=` gain a 512-character raw-length cap on `/my`; `?from=` previously had no explicit cap (`parseSelection` bounds the resolved list, but not the raw string) | Defensive parity with the `?client=` 80-char cap from 7.4; every legitimate id list fits well inside it |
| 2026-07-26 | Register reading-copy exports (CSV, TXT, print-to-PDF) added to Backup, below the register file, never counting as a verified export and unavailable while locked; CSV ships OWASP formula-injection escaping | Rocky's 26 Jul direction; the file-is-primary doctrine and backup-first UX must not be weakened by convenience copies, and register fields are user-entered text that spreadsheets would otherwise execute |
| 2026-07-27 | Costs (screenCosts) excludes `planned` rows from the renewal lists and the monthly/annual totals, beyond section 16's literal list | A planned account is not being paid for by definition; leaving it in the ledger would overstate real spend the same way counting it in the Overview risk tiles would overstate real risk |
| 2026-07-30 | PRD v1.5: section 18 (Answer engine and search visibility) added; section 16 layout replaced by the compact landing with fully collapsed category shelves; the Phase 12 motion inventory replaced by the seven-item spec with `--ease-swift`/`--ease-spring` tokens | Rocky's 29 Jul direction: the measured 92,000px opening page, imperceptible motion, and AI answer engines seeing no directory content because they execute no JavaScript |
| 2026-07-30 | Static crawler content block generated into `index.html` between `seo-static` marker comments by dev-time `scripts/build-seo.mjs`, hidden by `js/data-loader.js` on app boot; `faq.html`, `sitemap.xml`, `llms.txt`, `data/category-intros.json` and the generator added beyond the PRD section 3 file list; CI gains a byte-identical drift gate | GPTBot, ClaudeBot and PerplexityBot do not render JS, so quotable content must exist in raw HTML; a committed artefact plus a drift gate keeps the no-build-step law intact while making staleness against `tools.json` impossible |
| 2026-07-30 | `scripts/smoke-test.mjs` CSP hash-drift gate skips `type="application/ld+json"` scripts | CSP treats JSON-LD as a non-executable data block: `script-src` never applies, so there is no hash to allow-list and nothing for the gate to protect; the gate still hashes every executable inline script |
| 2026-07-30 | View Transitions API admitted to the section 16 implementation constraints, feature-detected and skipped under reduced motion | The motion inventory uses browser-native View Transitions for filter redraws, theme toggle and the deck-open morph; no library, and unsupported browsers keep the previous behaviour |
| 2026-07-30 | PRD section 18 gains per-tool questions: one generated Q&A per active tool, composed at generation time from free_limit, description, paid_from and alternatives, surfaced in the static crawler block and on the ?tool= permalink, excluded from FAQPage JSON-LD | Rocky's direction that every tool should carry an FAQ question; the long-tail phrasing people and answer engines actually use is per-tool, and the dataset already holds the honest answers so generation keeps maintenance at zero |
| 2026-07-31 | PRD section 16's first-viewport clause reconciled: the search input stays inside the 812px viewport, the first shelf header's top budget is 880px at 375x812 (measured 863px as built), superseding "first shelf rows visible within the first mobile viewport" | The mandated hero trust signals and ways-in band honestly fill most of the first phone screen; the 14.1 fix round recovered 322px (chip scroll row, toolbar fold, and a design-system line-height leak on bare p elements worth more than both named levers) and the remaining 51px could only be closed by cutting content the same spec mandates. One thumb-flick beats one full screen, and the number is now pinned and tested |
| 2026-07-31 | Tool 75 (Coursera / Khan Academy) paid_from corrected from 0 to 47 and scales_with to features; PRD section 18's per-tool answer length becomes a 40-80 word aim with smoke-enforced 30-100 hard bounds; summary elements join the site-wide focus-visible rule | The 14.3 verifier caught the generated answer contradicting itself (no paid tier to outgrow beside a £47/month subscription named in the same paragraph) because the field was wrong, not the generator; ten answers legitimately fall outside the cosmetic 40-80 range because free_limit is quoted verbatim and truncating it would cost honesty; the new FAQ summaries fell back to the browser's native focus outline |
